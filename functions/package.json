{
  "name": "functions",
  "scripts": {
    "test": "npm run lint && npm run build && node --require ./test/setup.cjs --test test/*.test.cjs",
    "lint": "eslint \"src/**/*\"",
    "clean": "node -e \"require('node:fs').rmSync('lib', {recursive: true, force: true})\"",
    "build": "npm run clean && tsc",
    "serve": "npm run build && node lib/scripts/stage-emulator-secrets.js && npm exec --yes --package firebase-tools@15.24.0 -- firebase emulators:start --only auth,firestore,functions",
    "shell": "npm run serve",
    "start": "npm run serve",
    "deploy": "npm exec --yes --package firebase-tools@15.24.0 -- firebase deploy --only functions",
    "logs": "npm exec --yes --package firebase-tools@15.24.0 -- firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/firestore": "8.7.0",
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.0"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "22.20.1",
    "@typescript-eslint/eslint-plugin": "8.65.0",
    "@typescript-eslint/parser": "8.65.0",
    "eslint": "10.8.0",
    "typescript": "5.9.3"
  },
  "overrides": {
    "rimraf": "6.1.3",
    "uuid": "11.1.1"
  },
  "private": true
}
