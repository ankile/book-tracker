{
  "name": "functions",
  "scripts": {
    "test": "npm run lint && npm run build && node --test test/*.test.cjs",
    "lint": "eslint \"src/**/*\"",
    "build": "tsc",
    "serve": "npm run build && npm exec --yes --package firebase-tools@15.24.0 -- firebase emulators:start --only functions",
    "shell": "npm run build && npm exec --yes --package firebase-tools@15.24.0 -- firebase functions:shell",
    "start": "npm run shell",
    "deploy": "npm exec --yes --package firebase-tools@15.24.0 -- firebase deploy --only functions",
    "logs": "npm exec --yes --package firebase-tools@15.24.0 -- firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/firestore": "8.6.0",
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.0"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "22.20.1",
    "@typescript-eslint/eslint-plugin": "8.64.0",
    "@typescript-eslint/parser": "8.64.0",
    "eslint": "10.7.0",
    "typescript": "5.9.3"
  },
  "private": true
}
