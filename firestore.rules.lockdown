rules_version = '2';

// Incident lockdown ruleset (README: "Abusive traffic and runaway spend").
// Deploy with the command in the README; redeploy firestore.rules to lift.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
